#include<stdio.h>
#include<conio.h>
#include<math.h>
int main()
{
    float a = 6.00;
    float b = 3.52;
    float b = -3.0;
    
    printf("\n %f",ceil(a));
    printf("\n %f",floor(a));
    printf("\n %f",ceil(b));
    printf("\n %f",floor(b));
    printf("\n %f",sqrt(a));
    printf("\n %f",pow(a,2));
    printf("\n %f",abs(a));
    
    return 0;
}